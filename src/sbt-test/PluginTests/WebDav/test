# check that webdav publish runs
# this doesn't actually work because:
# Skipping Private Repository mount as this is a pull-request build
# > webdav:publish
> inspect webdav:publish

# check that webdav mkcol exists
> inspect webdav:mkcol
